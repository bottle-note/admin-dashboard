# 주류 통계 화면 검증

Final result: passed

## 기준

- 선택한 검색 목록 + 상세 분석 구성을 유지하고, 색상·타이포그래피·카드·버튼은 기존 어드민 컴포넌트를 사용하라는 사용자 요청을 적용했다.
- 이미지 기준: `/Users/eva/.codex/generated_images/01a09a4b-ef3d-7ae1-b9b5-96d010dbaa95/exec-710583b0-68c3-44dd-a1bf-4680b4602064.png` (1488 × 1058).
- 로컬 검증 캡처(저장소 미포함): `output/statistics-design/desktop.png` (1488 × 1058), `charts.png`, `mobile.png` 및 `mobile-charts.png` (모바일 390 × 844).
- 기준 이미지와 실제 데스크톱 캡처를 함께 비교하고, 차트 및 모바일 캡처를 별도로 확인했다. 이미지의 가상 수치 대신 API 응답을 사용하므로 수치와 검색 결과는 다르다.

## 확인 결과

- 구성: 왼쪽 검색과 오른쪽 선택 주류 분석, 네 개 요약 지표, 지표 그룹 전환을 유지했다. 기존 공통 필터 카드는 상단에 배치했다.
- 스타일: 기존 Card, Button, Input, Select와 차트 컴포넌트의 글꼴·간격·테두리·상태 색상을 사용한다. 원본 목업의 장식과 색상 차이는 사용자 요청에 따른 변경이다.
- 이미지와 아이콘: 실제 주류 이미지와 기존 Lucide 아이콘을 사용한다. 긴 주류명은 줄바꿈하며 카드 밖으로 벗어나지 않는다.
- 차트: 구간 발생량은 막대, 누적·현재 상태는 선으로 표현한다. 요약 계산은 구간 합계와 마지막 시점 값을 구분하되, 사용자 요청에 따라 기준 설명 표기는 제거했다. 데이터가 없으면 0으로 대체하지 않는다.
- 반응형: 데스크톱과 390px 모바일에서 정렬·줄바꿈·차트·스크롤을 확인했다. 모바일 문서 너비와 뷰포트 너비가 모두 390px로 가로 넘침이 없었다.
- 동작: 실제 API를 사용하는 통계 E2E 5개가 통과했다. 검색·선택 유지·요약 수치·그룹 전환·기간·비교 대상 제한·URL 복원을 검증했다.
- 브라우저 오류 로그는 없었다. lint는 오류 없이 기존 경고 7개, build는 통과했다.

확인한 범위에서 겹침·넘침은 없었다. 탭별 차트 분리 구성의 직관성은 후속 사용자 검토가 필요하다. 서버 오류 상태의 시각 검증, 별도 태블릿 뷰포트, 확대 및 보조 기술 검증은 수행하지 않았다.

## 후속 수정 검증

- 검색 패널 sticky의 추가 80px을 제거하고 본문 기본 간격에 맞췄다. 초기 정렬과 스크롤 중·끝 위치를 브라우저에서 확인했다.
- 반복 캡션을 제거하고 찜 해제 상태 도움말의 키보드 접근을 확인했다. 390px 모바일에서 가로 넘침이 없었다.
- 요약 집계 기준 문구 제거 후 데스크톱 화면을 확인했고, lint(기존 경고 7개), build, 통계 E2E 5개가 통과했다.
