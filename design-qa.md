# 벌크 이미지 화면 검증

- 구현: `http://localhost:5173/whisky/excel-bulk`
- 초기 로그인 차단은 이후 환경 설정으로 해결되었다. 아래는 로그인한 Chromium에서 실제 화면을 조작한 후속 검증 기록이다.
- 검증 화면: 행별 compact 이미지, 기존 이미지 크롭 편집, 파일명 준비·다중 매칭, 최종 전송 상태, 접힌 작업 정보.
- 기본 UI에는 작은 이미지와 동작 버튼을 유지한다. 좌표 입력과 JSON은 펼친 상태에서만 상세 내용을 노출한다.
- 파일명 준비 목록은 자체 스크롤을 만들지 않는다. 89행을 사용한 기존 Chrome 확인과 이번 좁은 화면의 DOM 검증에서 모달 전체 스크롤을 확인했다.
- 1280×720 및 480×900 뷰포트에서 크롭 좌표 입력, 버튼, 여러 이미지 모달을 확인했다. 좁은 화면에서 좌표는 2열로 배치되며 모달의 가로 넘침이 없다. 내용이 길면 전체 모달을 스크롤해 저장·취소에 접근한다.
- JSON 영역은 읽기 전용이며 접힌 상태에서는 내용을 렌더하지 않는다. 전체 행 상태를 반영하고 파일명과 이미지 URL은 화면의 현재 데이터에서 가져온다.

## 검증 증거

벌크 도메인 Playwright의 `test-results/whisky-excel-bulk-행별-이미지-교체와-파일명-매칭-결과를-최종-전송에-반영한다-chromium/`:

- `crop-editor.png`: 펼친 좌표 입력과 저장 버튼.
- `crop-editor-narrow.png`: 좁은 화면의 2열 입력.
- `image-matching.png`: 파일명 목록 다운로드와 기존 매칭 흐름.
- `image-matching-narrow.png`: 전체 모달 스크롤, 매칭 결과와 하단 버튼.
- `bulk-image-table.png`: 성공 이미지와 compact 동작 버튼.
- `bulk-work-state.png`: 전송 상태와 펼친 작업 정보.

실제 Excel 검증과 S3 업로드를 사용한다. 최종 등록 응답과 의도적인 presign 실패 1회는 테스트에서 대체한다. 벌크 API로 실제 테스트 위스키는 등록하지 않는다.

## 남아 있는 환경 제한

CloudFront가 CORS 헤더를 제공하지 않는 기존 URL 이미지는 브라우저에서 재다운로드해 크롭할 수 없다. 현재 세션에서 업로드한 개별·다중 이미지는 보관된 파일로 편집할 수 있으며 검증했다. URL만 있는 경우 오류 안내 후 `교체`에서 원본 파일을 선택할 수 있다. URL만으로 편집하려면 CDN CORS 설정 변경이 필요하다.

최종 결과: lint/build 통과, 벌크 E2E 5개 통과(인증 setup 포함). 공통 크롭 기존 테스트 4개 및 배너 이미지 미리보기 E2E 통과. 캡처한 데스크톱·좁은 화면에서 새 UI의 겹침과 가로 넘침 없이 입력·스크롤·저장 버튼에 접근 가능함을 확인했다. 위 CDN 제한은 별도로 유지된다.
