# BottleNote Admin Design Guide

이 문서는 BottleNote Admin에서 새 화면을 기획하고 기존 화면을 확장할 때 사용하는 UI 디자인 기준이다.
기능 요구사항이나 API 계약을 반복해서 설명하지 않고, 화면 구조·배치·시각적 위계·컴포넌트 선택을 빠르게 결정하는 데 집중한다.

## 1. Visual Direction

- 현재의 shadcn/ui 기반 중립 색상 체계와 semantic token을 사용한다.
- 색상은 상태, 위험, 선택, 주요 행동을 구분하는 데 사용한다. 장식만을 위한 새 색상은 추가하지 않는다.
- 페이지 제목, 섹션 제목, 필드 라벨, 보조 정보 순으로 위계를 만든다.
- 제목이나 라벨만으로 의미가 충분하면 같은 내용을 설명문으로 반복하지 않는다.
- 아이콘은 텍스트의 의미를 보조한다. 아이콘만으로 새로운 의미를 만들지 않는다.

### 기본 시각 리듬

- 페이지 제목은 기존 `text-2xl font-bold` 패턴을 따른다.
- 페이지의 큰 영역 사이는 24px, 한 영역 안의 요소 사이는 주로 16px, 라벨과 입력 요소 사이는 8px을 기준으로 한다.
- Border와 Card는 정보 그룹을 나누는 데 사용한다. 모든 요소를 Card로 감싸지 않는다.
- 목록과 통계 화면은 가용 너비를 사용하고, 입력 폼은 짧은 필드가 불필요하게 넓어지지 않도록 grid나 Card로 폭을 나눈다.

색상과 radius의 구현 기준은 `src/index.css`와 `tailwind.config.js`를 따른다.

## 2. Page Frame

전체 화면은 `src/components/layout/AdminLayout/index.tsx`의 Header + Sidebar + 내부 스크롤 Main 구조를 따른다.
페이지 내부는 아래 순서로 구성한다.

```text
┌ 제목 · 상태 · 설명                         주요 액션 ┐
├ 검색 · 필터 · 정렬 등 화면 제어 영역                 ┤
├ 핵심 콘텐츠                                           ┤
└ 결과 수 · 페이지 크기                       페이지 이동 ┘
```

- 새 목록·대시보드 화면은 `PageHeader`를 우선 사용한다.
- 생성·수정·상세 화면은 뒤로가기와 액션을 포함한 `DetailPageHeader`를 우선 사용한다.
- 좁은 화면에서는 제목과 액션, 검색과 필터를 세로로 배치한다.
- 넓은 화면에서는 제목과 액션을 양쪽에 두고, 관련 필드는 필요한 범위에서만 다열로 배치한다.
- 헤더 액션은 줄바꿈될 수 있어야 하며 콘텐츠 너비를 밀어내지 않아야 한다.

참고 컴포넌트:

- `src/components/common/PageHeader.tsx`
- `src/components/common/DetailPageHeader.tsx`
- `src/components/layout/AdminLayout/index.tsx`

## 3. Page Types

화면을 새로 구성하기 전에 가장 가까운 유형과 대표 화면을 선택한다.

### 일반 목록

```text
[페이지 제목]                                    [등록]
[검색 입력________________] [필터] [정렬] [검색]
[테이블___________________________________________]
[현재 표시 범위] [페이지 크기]           [이전] [다음]
```

- 대표 화면: `src/pages/distilleries/DistilleryList.tsx`
- 검색 입력은 남는 너비를 사용하고, 필터와 정렬은 내용에 맞는 고정 폭을 사용한다.
- 텍스트 검색은 Enter 또는 검색 버튼으로 적용하고, 선택형 필터는 선택 즉시 적용할 수 있다.
- 검색·필터·정렬·페이지 상태는 URL에서 복원 가능한 형태를 기본으로 한다.

### 복합 검색·작업 목록

- 대표 화면: `src/pages/mfds/MfdsDeclarationList.tsx`
- 검색과 주요 필터를 첫 줄에, 추가 필터와 현재 결과에 대한 액션을 다음 줄에 둘 수 있다.
- 화면 전체에 영향을 주는 모드는 테이블 위 안내 영역으로 현재 상태와 완료 방법을 보여준다.
- 결과 전체를 대상으로 하는 액션은 어떤 데이터 범위를 사용하는지 버튼 가까이에 표시한다.

### 생성·수정 폼

```text
[←] [등록/상세 제목 · 상태]                  [삭제] [저장]
┌ 기본 정보 Card ─────────────────┐ ┌ 미디어 Card ┐
│ 관련된 짧은 필드 2열             │ │ 미리보기     │
│ 긴 입력 필드 전체 폭             │ │ 업로드       │
└─────────────────────────────────┘ └─────────────┘
┌ 연관 정보 또는 추가 설정 Card ────────────────────┐
└───────────────────────────────────────────────────┘
```

- 대표 화면: `src/pages/distilleries/DistilleryDetail.tsx`, `src/pages/banners/BannerDetail.tsx`
- 정보 성격별로 Card를 나누되, 필드 하나마다 Card를 만들지 않는다.
- 짧고 서로 연관된 필드는 2열, 긴 텍스트와 복합 입력은 전체 폭을 사용한다.
- 이미지나 결과 미리보기가 판단에 필요하면 입력 영역과 나란히 배치한다.
- 등록과 수정 화면은 가능한 한 같은 레이아웃을 공유해 위치 학습을 유지한다.

### 목록 안의 짧은 작업

- 대표 화면: `src/pages/inquiries/InquiryList.tsx`
- 현재 목록 맥락을 유지한 채 확인·답변처럼 짧게 끝나는 작업은 Dialog를 사용할 수 있다.
- 비교, 여러 섹션 편집, 긴 스크롤이 필요하면 별도 상세 화면을 사용한다.
- Dialog 안에서도 제목, 현재 상태, 본문, 입력, 액션 순서를 유지한다.

### 대시보드·통계

- 대표 화면: `src/pages/Dashboard.tsx`, `src/pages/statistics/VisitorStatistics.tsx`
- 핵심 수치는 작은 요약 Card, 추이는 차트 Card로 구분한다.
- 조회 조건은 결과와 분리된 하나의 Card에 모은다.
- 같은 범주의 지표는 크기와 제목 위치를 맞춰 비교하기 쉽게 구성한다.
- 차트는 Card 너비 안에서 축소되며 가로 overflow가 생기지 않아야 한다.

### 일괄 처리

- 대표 화면: `src/pages/whisky/WhiskyExcelBulkPage.tsx`
- 파일 선택, 검증 결과, 실행, 완료 결과를 단계 순서대로 배치한다.
- 현재 단계와 다음 행동을 화면에서 바로 확인할 수 있게 한다.
- 실제 반영 전에 처리 범위와 실패 항목을 확인할 수 있는 영역을 둔다.

## 4. Header and Actions

- 대표 액션은 헤더 우측 마지막에 primary 버튼으로 둔다.
- 보조 액션은 outline, 삭제처럼 되돌리기 어려운 액션은 destructive를 사용한다.
- 한 화면에서 primary로 보이는 액션을 여러 개 만들지 않는다.
- 수정 화면에서는 뒤로가기, 제목·상태, 삭제·저장 순으로 읽히게 한다.
- 처리 중에는 버튼을 비활성화하고 `저장 중...`, `등록 중...`처럼 현재 작업을 동사로 표시한다.
- 긴 폼에서 하단 액션이 실제로 필요한 경우에만 별도 footer나 sticky action을 검토한다. 기본적으로 헤더 액션을 중복하지 않는다.

## 5. Lists and Tables

- 목록 행 전체 클릭을 상세 이동의 기본 패턴으로 사용한다.
- 이동 가능한 행은 hover 배경, pointer cursor, 주요 텍스트의 시각적 강조로 목적지를 명확히 한다.
- 행 안의 Switch, 버튼, 링크 등 독립 조작은 행 이동과 충돌하지 않게 이벤트를 분리한다.
- 행마다 단순 `보기` 버튼을 반복하기보다 행 전체 이동을 우선한다. 한 행에 목적지가 여러 개일 때만 명시적 액션을 사용한다.
- 숫자는 비교하기 쉽도록 우측 정렬하고, ID나 짧은 수치는 필요할 때 monospace를 사용한다.
- 상태는 텍스트와 Badge를 함께 사용한다. 색상만으로 상태를 전달하지 않는다.
- 많은 컬럼이 필요한 표는 임의로 컬럼을 숨기기보다 가로 스크롤 영역을 사용한다.
- 어떤 데이터를 목록 컬럼으로 보여줄지는 기능 기획 시 사용자와 결정하며, 디자인 가이드가 임의로 추가하거나 생략하지 않는다.
- 데이터가 없을 때는 기본 빈 목록과 검색 결과 없음의 문구를 구분한다.

참고 컴포넌트:

- `src/components/common/Pagination.tsx`
- `src/components/common/StatusToggle.tsx`
- `src/components/ui/table.tsx`
- `src/components/ui/badge.tsx`

## 6. Search and Filters

- 검색 아이콘은 입력창 내부 왼쪽에 두고 입력 내용이 겹치지 않게 한다.
- 검색 입력은 제어 영역의 남는 너비를 사용한다.
- 옵션 수가 적고 고정되어 있으면 `Select`, 옵션이 많거나 이름 검색이 필요하면 `SearchableSelect`를 사용한다.
- 필터 라벨이 없으면 trigger 자체만으로 대상이 분명해야 한다. 분명하지 않으면 짧은 라벨을 위에 둔다.
- 기본값인 `전체`는 명시하되 URL에는 불필요한 기본값을 남기지 않는다.
- 필터가 바뀌면 첫 페이지로 돌아간다.
- 화면 제어가 특정 모드에서 유효하지 않다면 숨기기보다 disabled와 모드 안내를 함께 사용한다.

참고 컴포넌트:

- `src/components/common/SearchableSelect.tsx`
- `src/components/ui/select.tsx`
- `src/components/ui/input.tsx`

## 7. Forms and Media

- 필드는 `FormField`를 사용해 라벨, 필수 표시, 오류 메시지 위치를 통일한다.
- 필수 표시는 라벨 바로 뒤에 두고, 오류는 해당 입력 바로 아래에 표시한다.
- placeholder는 예시나 입력 형식을 보조하며 라벨을 대신하지 않는다.
- 조건에 따라 나타나는 필드는 그 조건을 제어하는 입력과 같은 Card 안에 둔다.
- 읽기 전용 정보는 disabled input으로 위장하지 않고 일반 텍스트, Badge, 요약 영역으로 표현한다.
- 업로드 화면은 선택한 파일이나 미디어의 미리보기를 제공하고, 업로드 중 상태를 입력 가까이에 표시한다.
- 이미지 편집이 필요한 기능은 실제 파일 크기와 절감률, 자유 비율 리사이즈, 선택 영역의 원본 px을 확인할 수 있게 한다.

참고 컴포넌트:

- `src/components/common/FormField.tsx`
- `src/components/common/MediaUpload.tsx`
- `src/components/ui/card.tsx`

## 8. Status, Feedback, and Confirmation

- 조회 전용 상태는 Badge, 즉시 변경 가능한 활성 상태는 `StatusToggle`을 우선한다.
- 성공·실패 알림은 toast를 사용하고, 필드 단위 오류는 해당 필드 가까이에 표시한다.
- 로딩은 데이터 영역에서, 재조회 중 상태는 기존 데이터를 유지한 채 제어 영역 가까이에서 보여준다.
- 삭제는 `DeleteConfirmDialog`를 사용한다.
- 확인 Dialog는 실행할 동작, 대상, 되돌릴 수 있는지를 명확히 구분한다.
- 다운로드·변환처럼 결과를 확인한 뒤 실행해야 하는 작업은 준비 완료 Dialog에서 대상 수와 결과 수를 보여줄 수 있다.

참고 컴포넌트:

- `src/components/common/DeleteConfirmDialog.tsx`
- `src/components/common/StatusToggle.tsx`
- `src/hooks/useToast.ts`

## 9. Responsive and Interaction

- 헤더 액션과 필터는 좁은 화면에서 줄바꿈하거나 세로로 쌓인다.
- 표는 셀 내용을 무리하게 압축하지 않고 `overflow-x-auto` 영역 안에 둔다.
- Popover와 Dialog는 trigger 너비, viewport 경계, 내부 스크롤을 고려한다.
- 아이콘 전용 버튼은 접근 가능한 이름을 제공한다.
- 클릭 가능한 행 안의 독립 조작, 검색 가능한 선택창, Drag & Drop처럼 연속 조작이 필요한 UI는 마우스와 키보드 흐름을 모두 고려한다.

## 10. Component Selection Index

| 목적               | 우선 컴포넌트         | 사용 기준                        |
| ------------------ | --------------------- | -------------------------------- |
| 목록·대시보드 헤더 | `PageHeader`          | 제목, 선택적 설명, 우측 액션     |
| 생성·수정 헤더     | `DetailPageHeader`    | 뒤로가기, 상태, 저장·삭제 액션   |
| 폼 필드            | `FormField`           | 라벨, 필수, 오류가 있는 입력     |
| 고정 옵션 선택     | `Select`              | 옵션이 적고 검색이 필요 없음     |
| 검색형 선택        | `SearchableSelect`    | 옵션이 많거나 이름 검색이 필요함 |
| 목록 이동          | `Pagination`          | 페이지·커서 기반 목록            |
| 활성 상태 변경     | `StatusToggle`        | 목록에서 즉시 활성/비활성 변경   |
| 삭제 확인          | `DeleteConfirmDialog` | 되돌리기 어려운 삭제             |
| 이미지·파일        | `MediaUpload`         | 업로드와 미리보기가 함께 필요함  |
| 시계열 데이터      | `TimeSeriesChart`     | 기간별 수치 변화 비교            |

공용 컴포넌트가 해결하지 못하는 화면별 동작은 해당 페이지 가까이에서 조합한다. 동일한 조합이 여러 도메인에서 반복될 때 공용화를 검토한다.

## 11. Screen Planning Prompt

새 화면을 논의할 때는 긴 스펙 대신 아래 항목만 먼저 정한다.

```text
화면 유형:
가장 가까운 기존 화면:
헤더 제목과 주요 액션:
검색·필터 또는 입력 영역 배치:
본문의 Card·Table·Chart 구성:
행 클릭·Dialog·별도 페이지 중 이동 방식:
모바일에서 달라질 배치:
사용자 판단이 필요한 항목:
```

합의된 내용은 구현과 테스트에 반영한다. 여러 화면에서 반복되는 새 UI 패턴이 확인되면 이 문서의 기존 항목을 보완한다.
