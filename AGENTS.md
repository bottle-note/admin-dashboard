# Bottlenote Admin Dashboard Agent Guide

## 1. 탐색과 구현

- 기능에 필요한 화면·API·주변 코드만 확인하고 구조가 파악되면 구현을 시작한다.
- 기존 위치·명명·데이터 흐름을 따라 현재 요구사항을 만족하는 가장 단순하고 읽기 쉬운 코드를 작성한다.
- 범위 밖 리팩터링과 예상 재사용을 위한 추상화를 하지 않는다.

## 2. 컴포넌트 재사용

- 공용 및 동일 도메인 컴포넌트를 먼저 확인하고, 같은 역할의 컴포넌트를 중복 구현하지 않는다.
- 도메인별 기능은 공용 컴포넌트를 감싸서 추가하고, 여러 도메인에서 같은 기능이 필요할 때만 공용 컴포넌트로 올리되, 이 방식이 더 복잡하면 별도 컴포넌트로 만든다.

## 3. 프로젝트 구조

- API는 `types → service → hook`, 폼은 React Hook Form + Zod, 목록 상태는 URL로 관리한다.
- API 계약은 `https://docs.bottle-note.com/#admin`을 기준으로 한다.

## 4. 테스트

- 페이지·도메인 규모 기능은 구현 전 핵심 happy path를 제안하고 사용자에게 확인받는다.
- 승인된 시나리오를 Vitest와 React Testing Library 통합 테스트로 먼저 실패시킨 뒤 Red → Green으로 구현한다.
- 작은 UI primitive와 단순 문구·스타일 변경에는 별도 테스트를 강제하지 않는다.
- 권한, 저장·삭제, 데이터 변환, 조건별 API payload, 재현된 버그처럼 실패 시 잘못된 데이터나 주요 업무 오류가 발생할 수 있는 경우에만 예외·회귀 테스트를 추가한다.
